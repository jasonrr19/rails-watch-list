<div class="container">
<h1>Add a new movie to a list!</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
<%= f.input :comment %>
<%= f.association :movie %>
<%= f.submit %>
<% end %>
</div>

<%= link_to "Back", list_path(@list), class: "btn btn-ghost" %>
