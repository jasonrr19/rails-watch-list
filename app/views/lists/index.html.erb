<%# <nav class="navbar sticky-top navbar-lewagon">
  <div class="container-fluid ">
    <a class="navbar-brand img-fluid"><%= image_tag("logo.png")%>  <%# </div>
</nav> %>
<h1>Movie lists for all tastes!</h1>

<%= link_to "Add a List", new_list_path, class: "btn btn-ghost" %>

<div class="container">
  <div class="cards">
    <% @lists.each do |list| %>
      <%= link_to list_path(list) do %>
        <div class="card-category border">
          <%= list.name %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
